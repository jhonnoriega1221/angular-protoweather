import { PwToggleDirective } from './pw-toggle.directive';

describe('PwToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new PwToggleDirective();
    expect(directive).toBeTruthy();
  });
});

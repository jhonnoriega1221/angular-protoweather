import { AutocompleteItemDirective } from './autocomplete-item.directive';

describe('AutocompleteItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteItemDirective();
    expect(directive).toBeTruthy();
  });
});

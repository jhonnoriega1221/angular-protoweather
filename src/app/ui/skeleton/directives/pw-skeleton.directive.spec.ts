import { PwSkeletonDirective } from './pw-skeleton.directive';

describe('PwSkeletonDirective', () => {
  it('should create an instance', () => {
    const directive = new PwSkeletonDirective();
    expect(directive).toBeTruthy();
  });
});

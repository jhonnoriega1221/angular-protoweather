import { PwFlatButtonDirective } from './pw-flat-button.directive';

describe('PwFlatButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PwFlatButtonDirective();
    expect(directive).toBeTruthy();
  });
});

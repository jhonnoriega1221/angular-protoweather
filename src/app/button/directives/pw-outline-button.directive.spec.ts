import { PwOutlineButtonDirective } from './pw-outline-button.directive';

describe('PwOutlineButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PwOutlineButtonDirective();
    expect(directive).toBeTruthy();
  });
});

import { PwTextButtonDirective } from './pw-text-button.directive';

describe('PwTextButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PwTextButtonDirective();
    expect(directive).toBeTruthy();
  });
});

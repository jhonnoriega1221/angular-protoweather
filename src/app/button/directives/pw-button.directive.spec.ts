import { PwButtonDirective } from './pw-button.directive';

describe('PwButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PwButtonDirective();
    expect(directive).toBeTruthy();
  });
});
